import { useEffect, useState } from 'react'; import { Lock } from 'lucide-react'; import { Button } from '@/components/ui/button'; import {   AlertDialog,   AlertDialogContent,   AlertDialogDescription,   AlertDialogFooter,   AlertDialogHeader,   AlertDialogTitle, } from '@/components/ui/alert-dialog'; import { Input } from '@/components/ui/input';  interface VncPasswordDialogProps {   open: boolean;   onOpenChange: (open: boolean) => void;   onConfirm: (password: string) => void; }  export function VncPasswordDialog({ open, onOpenChange, onConfirm }: VncPasswordDialogProps) {   const [password, setPassword] = useState('');    useEffect(() => {     if (!open) setPassword('');   }, [open]);    return (     <AlertDialog open={open} onOpenChange={onOpenChange}>       <AlertDialogContent>         <AlertDialogHeader>           <AlertDialogTitle>VNC 瀵嗙爜</AlertDialogTitle>           <AlertDialogDescription>             璇疯緭鍏ヨ繙绋嬫闈㈢殑杩炴帴瀵嗙爜           </AlertDialogDescription>         </AlertDialogHeader>         <div className="relative">           <Lock className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" />           <Input             type="password"             placeholder="杈撳叆瀵嗙爜"             value={password}             onChange={(e) => setPassword(e.target.value)}             className="pl-9"             autoFocus             onKeyDown={(e) => {               if (e.key === 'Enter' && password) {                 onConfirm(password);                 setPassword('');               }             }}           />         </div>         <AlertDialogFooter>           <Button variant="outline" onClick={() => { onOpenChange(false); setPassword(''); }}>             鍙栨秷           </Button>           <Button disabled={!password} onClick={() => { onConfirm(password); setPassword(''); }}>             杩炴帴           </Button>         </AlertDialogFooter>       </AlertDialogContent>     </AlertDialog>   ); }